<!-- Validation failure messages -->

<% if @book_keyword.errors.any? %>
  <% @book_keyword.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<!-- Responsive panel example -->

<div class="row">
  <div class="col-sm-offset-1 col-sm-10 col-md-offset-2 col-md-8">
    <div class="panel panel-default">
      <div class="panel-heading">
        <h3 class="panel-title">Edit Book Keyword #<%= @book_keyword.id %></h3>
      </div>
      <div class="panel-body">
        <form action="/update_book_keyword/<%= @book_keyword.id %>" method="post" class="form-horizontal">
          <!-- Hidden input for authenticity token to protect from forgery -->
          <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

          <!-- Label and input for keyword_id -->
          <div class="form-group">
            <label for="keyword_id" class="col-sm-2 control-label">
              Keyword
            </label>
            <div class="col-sm-10">
              <input type="text" id="keyword_id" name="keyword_id" placeholder="Keyword" class="form-control" value="<%= @book_keyword.keyword_id %>">
            </div>
          </div>

          <!-- Label and input for book_id -->
          <div class="form-group">
            <label for="book_id" class="col-sm-2 control-label">
              Book
            </label>
            <div class="col-sm-10">
              <input type="text" id="book_id" name="book_id" placeholder="Book" class="form-control" value="<%= @book_keyword.book_id %>">
            </div>
          </div>

          <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
              <button class="btn btn-warning">
                Update Book Keyword
              </button>
              or
              <a href="/book_keywords/<%= @book_keyword.id %>">Cancel</a>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>


<!-- End responsive panel example -->
